# OS / tools
.DS_Store
Thumbs.db

# Python
__pycache__/
*.pyc

# Archives / binaries (we ship via EREP snapshots, not Git)
*.zip
*.parquet

# Local env
.env
.env.*

@'
# VS Code (shared workspace files)
.vscode/*
!.vscode/tasks.json
!.vscode/settings.json
!.vscode/extensions.json
'@ | Out-File -FilePath .gitignore -Encoding utf8 -Append
# VS Code (shared workspace files)
.vscode/*
!.vscode/tasks.json
!.vscode/settings.json
!.vscode/extensions.json
