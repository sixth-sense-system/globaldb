# OS / tools
.DS_Store
Thumbs.db

# Python
__pycache__/
*.pyc

# Archives / binaries (we ship via EREP snapshots, not Git)
*.zip
*.parquet

# Local env (plaintext never committed)
.env
.env.*
!.env.*.enc

# VS Code (shared workspace files)
.vscode/*
!.vscode/tasks.json
!.vscode/settings.json
!.vscode/extensions.json

# sops/age local state
**/*.dec
**/*.decrypted
**/*.plaintext
**/*.unencrypted
